---

- name: install ansible galaxy roles from internal sources
  command: "ansible-galaxy install --role-file={{ prelude_roles_file_path }}/{{ prelude_roles_file }} --roles-path={{ prelude_roles_directory_path }} --no-deps"
